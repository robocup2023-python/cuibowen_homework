字典中的键必须是不可变的，而值可以是任意的数据类型。
dict = {
    [1, 1]: 0,
    [2, 2]: 1,
    [3, 3]: 2
}
在该部分中，字典的键为列表，是可变的类型，不可以作为字典的键
dict = {
    {1, 1}: 0,
    {2, 2}: 1,
    {3, 3}: 2
}
在该部分中，字典的键为集合，也是可变类型，同样不可以作为字典的键

可变的数据类型通常包括列表，字典，集合等，而不可变的类型主要为元组，字符串和整数及浮点数
